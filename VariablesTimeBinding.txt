GamePlayCameraMode => _gameplayCameraMode != null ? _gameplayCameraMode : GameData.DefaultCameraMode
// связывание происходит на этапе выполнения команды, так как необходимо, чтобы геймдизайнеры
// могли настраивать это переменную. В каждом моде можно переопределить режим для камеры,
// иначе подставить дефолтный указать в настройках
WindowPrefabPath => "ProjectResourcesPath"
// путь в проекте для окна, связывание происходит при старте работы программы,
// так как этот путь не может изменяться в течение работы программы
EntityActor = actor
// связывание происходит при каждом создании актора для данной сущности с помощью вызова метода
// OnActorCreated, сделано таким образом, так как сущность может быть создана без актора (например из сейва)
// без старта игры непосредственно, также актор может пересоздаться, например при перезагрузке уровня